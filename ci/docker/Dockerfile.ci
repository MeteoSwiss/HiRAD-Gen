FROM /capstor/scratch/cscs/pstamenk/corr_diff.sqsh
    
# setup
RUN apt-get update && apt-get install python3-pip python3-venv -y
RUN pip install --upgrade pip
    
# Install the rest of dependencies.
RUN pip install .





