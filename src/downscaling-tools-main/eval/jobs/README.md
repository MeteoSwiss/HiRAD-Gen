# Eval Jobs

This directory contains HPC orchestration code for the evaluation suite.

- `pipeline.py`, `renderer.py`, and `resources.py` render the canonical multi-stage SLURM flows.
- `templates/README.md` documents repo-owned sbatch templates.
- `legacy_docs/` contains pre-refactor playbooks kept for historical reference only.
