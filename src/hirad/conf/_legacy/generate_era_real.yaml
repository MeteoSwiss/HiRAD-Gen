hydra:
  job:
    chdir: true
    name: generation_era_real_dit_JJA2023
  run:
    dir: /capstor/scratch/cscs/pstamenk/outputs/generation/${hydra:job.name}

# Get defaults
defaults:
  - _self_
  # Dataset
  - dataset/anemoi_era_real_inference

  # Sampler
  - sampler/stochastic
  #- sampler/deterministic

  # Generation
  - generation/era_real