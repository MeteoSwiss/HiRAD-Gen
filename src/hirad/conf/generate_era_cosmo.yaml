hydra:
  job:
    chdir: true
    name: generation_regression_valid
  run:
    dir: /capstor/scratch/cscs/pstamenk/outputs/${hydra:job.name}

# Get defaults
defaults:
  - _self_
  # Dataset
  - dataset/era_cosmo

  # Sampler
  - sampler/stochastic
  #- sampler/deterministic

  # Generation
  - generation/era_cosmo
    #- generation/patched_based