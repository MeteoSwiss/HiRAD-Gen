hydra:
  job:
    chdir: true
    name: generation
  run:
    dir: ./outputs/${hydra:job.name}

# Get defaults
defaults:

  # Dataset
  - dataset/era_cosmo

  # Sampler
  - sampler/stochastic
  #- sampler/deterministic

  # Generation
  - generation/era_cosmo
    #- generation/patched_based