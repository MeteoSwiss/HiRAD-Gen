hydra:
  job:
    chdir: true
    name: generation_era_real_results
    # name: july2020
  run:
    dir: ./outputs/generation/${hydra:job.name}

# Get defaults
defaults:
  - _self_
  # Dataset
  - dataset/era_real_inference

  # Sampler
  - sampler/stochastic
  #- sampler/deterministic

  # Generation
  - generation/era_real
    #- generation/patched_based