hydra:
  job:
    chdir: true
    name: generation_era_real2km_results
  run:
    dir: /capstor/scratch/cscs/mmcgloho/outputs/generation/${hydra:job.name}

# Get defaults
defaults:
  - _self_
  # Dataset
  - dataset/anemoi_era_real_inference

  # Sampler
  - sampler/stochastic
  #- sampler/deterministic

  # Generation
  - generation/era_real2km