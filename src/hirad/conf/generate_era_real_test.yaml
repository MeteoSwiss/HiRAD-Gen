hydra:
  job:
    chdir: true
    name: generation_era_real_test
  run:
    dir: ./outputs/generation/${hydra:job.name}

defaults:
  - _self_
  - dataset/anemoi_era_real_inference
  - sampler/stochastic
  - generation/era_real_test
