hydra:
  job:
    chdir: true
    name: generation_ifsn320_real_results
  run:
    dir: ./outputs/generation/${hydra:job.name}

# Get defaults
defaults:
  - _self_
  # Dataset
  - dataset/anemoi_ifsn320_real_inference_1h
#  - dataset/anemoi_ifsn320_real_inference_6h

  # Sampler
  - sampler/stochastic
  #- sampler/deterministic

  # Generation
  - generation/ifsn320_real
